# Xctopus Requirements
# Optimized for compatibility with Google Colab and local environments
#
# IMPORTANT NOTES:
# - NumPy <2.0: Required to avoid binary incompatibility issues in Colab
# - PyTorch: Not included (Colab has it preinstalled; install separately for local: pip install torch)
# - Versions are conservative to ensure compatibility across environments
torch>=2.0.0,<2.5.0
# Core - NumPy <2.0 for binary compatibility (Colab compatibility)
numpy>=1.26.0,<2.0
pandas>=2.1.0
scikit-learn>=1.0.0,<1.6
pyarrow>=12.0.0

# AI/ML - Compatible versions (tested in Colab)
transformers>=4.0.0
peft>=0.12.0
sentence-transformers>=2.0.0
faiss-cpu>=1.13.0

# Performance & UI
tqdm>=4.60.0
rich>=13.0.0
psutil>=5.0.0

# Backend
huggingface-hub>=0.20.0
safetensors>=0.4.0
tokenizers>=0.15.0